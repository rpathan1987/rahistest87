name: Hello World Test

on:
  workflow_dispatch:
  push:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Run Hello Script
        run: |
          chmod +x hello.sh
          ./hello.sh
